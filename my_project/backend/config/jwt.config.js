module.exports = {
  secret: 'yourSuperSecretKeyForJWT', // Replace with a strong, unique secret, ideally from an environment variable
  expiresIn: '1h', // Token expiration time (e.g., 1 hour, 7 days)
};
